# Production Environment Configuration for Railway
NODE_ENV=production

# Database - Railway will provide this via environment variables
MONGODB_URI=${MONGODB_URI}

# JWT Secret - Set this in Railway environment variables
JWT_SECRET=${JWT_SECRET}

# Server Port - Railway sets this automatically
PORT=${PORT}

# WhatsApp Integration - Disabled in production for stability
DISABLE_WHATSAPP=true

# Puppeteer Configuration - Not needed when WhatsApp is disabled
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable